# ssyworld

## 프로젝트 개요
- `index.html` 단일 파일로 구성된 정적 부동산 입지 분석 대시보드 (빌드 도구 없음, 순수 HTML/CSS/JS)
- 배포/서버 설정 없음 — 파일을 직접 열거나 정적 호스팅으로 서빙

## 폴더 단위 설정 (.claude/settings.json)
- 이 프로젝트는 단순 텍스트 파일 편집이며 git으로 완전히 되돌릴 수 있는 저위험 작업이므로
  파일 읽기/편집 도구(Read/Edit/Write/Grep/Glob)와 로컬 안전 git 명령(status/diff/log/add/commit 등)은 자동 허용
- push/reset/rebase/checkout/clean 등 되돌리기 어렵거나 원격에 영향을 주는 명령은 프롬프트 유지
- 브라우저 자동화, 외부 MCP 도구는 이 프로젝트에 불필요 — 추가하지 않음
- 다른 성격의 폴더(예: 다중 페이지 웹앱, 빌드 파이프라인이 필요한 프로젝트)를 이 저장소에 추가할 경우,
  해당 폴더 전용 `.claude/settings.json` / `.mcp.json`을 별도로 구성할 것 (전역 설정에 의존하지 않음)

## 작업 중 발견한 규칙 (점진적으로 추가)
- (신규 시행착오·규칙은 여기 불릿으로 한 줄씩 추가)
